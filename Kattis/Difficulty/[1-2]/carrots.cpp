#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,p;
    string s;
    scanf("%d %d",&n, &p);
    getline(cin,s);
    while(n--){
        getline(cin,s);
    }
    printf("%d",p);
    return 0;
}